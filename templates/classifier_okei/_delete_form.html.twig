<form method="post" action="{{ path('app_classifier_o_k_e_i_delete', {'id': classifier_o_k_e_i.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classifier_o_k_e_i.id) }}">
    <button class="btn">Delete</button>
</form>
