
<div id="delete-nomenclature" class="modal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body p-0">
                <div class="p-5 text-center"><i data-lucide="x-circle" class="w-16 h-16 text-danger mx-auto mt-3"></i>
                    <div class="text-3xl mt-5">Вы уверены?</div>
                    <div class="text-slate-500 mt-2">Вы точно хотите удалить номенклатуру {{ nomenclature.nomenclatureName }} ID: {{ nomenclature.id }}? <br>Данную операцию невозможно отменить.
                    </div>
                </div>
                <div class="px-5 pb-8 text-center">
                    <form method="post" action="{{ path('app_nomenclature_delete', {'id': nomenclature.id}) }}">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nomenclature.id) }}">
                        <button type="button" data-tw-dismiss="modal" class="btn btn-outline-secondary w-24 mr-1">Отмена
                        </button>
                        <button type="submit" class="btn btn-danger w-24">Удалить</button>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div> <!-- END: Modal Content -->

