{% extends 'base.html.twig' %}

{% block title %}ProductionType{% endblock %}

{% block body %}
    <h1>ProductionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ production_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ production_type.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_production_type_index') }}">back to list</a>

    <a href="{{ path('app_production_type_edit', {'id': production_type.id}) }}">edit</a>

    {{ include('production_type/_delete_form.html.twig') }}
{% endblock %}
