<form method="post" action="{{ path('app_production_recipe_structure_delete', {'id': production_recipe_structure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production_recipe_structure.id) }}">
    <button class="btn">Delete</button>
</form>
